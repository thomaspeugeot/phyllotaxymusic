// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SpiralRhombusAPI } from './spiralrhombus-api'
import { SpiralRhombus, CopySpiralRhombusToSpiralRhombusAPI } from './spiralrhombus'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'
import { RhombusAPI } from './rhombus-api'

@Injectable({
  providedIn: 'root'
})
export class SpiralRhombusService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SpiralRhombusServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private spiralrhombussUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.spiralrhombussUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/spiralrhombuss';
  }

  /** GET spiralrhombuss from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI[]> {
    return this.getSpiralRhombuss(GONG__StackPath, frontRepo)
  }
  getSpiralRhombuss(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<SpiralRhombusAPI[]>(this.spiralrhombussUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SpiralRhombusAPI[]>('getSpiralRhombuss', []))
      );
  }

  /** GET spiralrhombus by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI> {
    return this.getSpiralRhombus(id, GONG__StackPath, frontRepo)
  }
  getSpiralRhombus(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.spiralrhombussUrl}/${id}`;
    return this.http.get<SpiralRhombusAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched spiralrhombus id=${id}`)),
      catchError(this.handleError<SpiralRhombusAPI>(`getSpiralRhombus id=${id}`))
    );
  }

  // postFront copy spiralrhombus to a version with encoded pointers and post to the back
  postFront(spiralrhombus: SpiralRhombus, GONG__StackPath: string): Observable<SpiralRhombusAPI> {
    let spiralrhombusAPI = new SpiralRhombusAPI
    CopySpiralRhombusToSpiralRhombusAPI(spiralrhombus, spiralrhombusAPI)
    const id = typeof spiralrhombusAPI === 'number' ? spiralrhombusAPI : spiralrhombusAPI.ID
    const url = `${this.spiralrhombussUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralRhombusAPI>(url, spiralrhombusAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralRhombusAPI>('postSpiralRhombus'))
    );
  }
  
  /** POST: add a new spiralrhombus to the server */
  post(spiralrhombusdb: SpiralRhombusAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI> {
    return this.postSpiralRhombus(spiralrhombusdb, GONG__StackPath, frontRepo)
  }
  postSpiralRhombus(spiralrhombusdb: SpiralRhombusAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralRhombusAPI>(this.spiralrhombussUrl, spiralrhombusdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted spiralrhombusdb id=${spiralrhombusdb.ID}`)
      }),
      catchError(this.handleError<SpiralRhombusAPI>('postSpiralRhombus'))
    );
  }

  /** DELETE: delete the spiralrhombusdb from the server */
  delete(spiralrhombusdb: SpiralRhombusAPI | number, GONG__StackPath: string): Observable<SpiralRhombusAPI> {
    return this.deleteSpiralRhombus(spiralrhombusdb, GONG__StackPath)
  }
  deleteSpiralRhombus(spiralrhombusdb: SpiralRhombusAPI | number, GONG__StackPath: string): Observable<SpiralRhombusAPI> {
    const id = typeof spiralrhombusdb === 'number' ? spiralrhombusdb : spiralrhombusdb.ID;
    const url = `${this.spiralrhombussUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SpiralRhombusAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted spiralrhombusdb id=${id}`)),
      catchError(this.handleError<SpiralRhombusAPI>('deleteSpiralRhombus'))
    );
  }

  // updateFront copy spiralrhombus to a version with encoded pointers and update to the back
  updateFront(spiralrhombus: SpiralRhombus, GONG__StackPath: string): Observable<SpiralRhombusAPI> {
    let spiralrhombusAPI = new SpiralRhombusAPI
    CopySpiralRhombusToSpiralRhombusAPI(spiralrhombus, spiralrhombusAPI)
    const id = typeof spiralrhombusAPI === 'number' ? spiralrhombusAPI : spiralrhombusAPI.ID
    const url = `${this.spiralrhombussUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SpiralRhombusAPI>(url, spiralrhombusAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralRhombusAPI>('updateSpiralRhombus'))
    );
  }

  /** PUT: update the spiralrhombusdb on the server */
  update(spiralrhombusdb: SpiralRhombusAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI> {
    return this.updateSpiralRhombus(spiralrhombusdb, GONG__StackPath, frontRepo)
  }
  updateSpiralRhombus(spiralrhombusdb: SpiralRhombusAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralRhombusAPI> {
    const id = typeof spiralrhombusdb === 'number' ? spiralrhombusdb : spiralrhombusdb.ID;
    const url = `${this.spiralrhombussUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SpiralRhombusAPI>(url, spiralrhombusdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated spiralrhombusdb id=${spiralrhombusdb.ID}`)
      }),
      catchError(this.handleError<SpiralRhombusAPI>('updateSpiralRhombus'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SpiralRhombusService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("SpiralRhombusService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
