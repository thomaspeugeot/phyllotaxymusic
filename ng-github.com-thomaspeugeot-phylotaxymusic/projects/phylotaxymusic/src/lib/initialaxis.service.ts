// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { InitialAxisAPI } from './initialaxis-api'
import { InitialAxis, CopyInitialAxisToInitialAxisAPI } from './initialaxis'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class InitialAxisService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  InitialAxisServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private initialaxissUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.initialaxissUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/initialaxiss';
  }

  /** GET initialaxiss from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI[]> {
    return this.getInitialAxiss(GONG__StackPath, frontRepo)
  }
  getInitialAxiss(GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<InitialAxisAPI[]>(this.initialaxissUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<InitialAxisAPI[]>('getInitialAxiss', []))
      );
  }

  /** GET initialaxis by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI> {
    return this.getInitialAxis(id, GONG__StackPath, frontRepo)
  }
  getInitialAxis(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.initialaxissUrl}/${id}`;
    return this.http.get<InitialAxisAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched initialaxis id=${id}`)),
      catchError(this.handleError<InitialAxisAPI>(`getInitialAxis id=${id}`))
    );
  }

  // postFront copy initialaxis to a version with encoded pointers and post to the back
  postFront(initialaxis: InitialAxis, GONG__StackPath: string): Observable<InitialAxisAPI> {
    let initialaxisAPI = new InitialAxisAPI
    CopyInitialAxisToInitialAxisAPI(initialaxis, initialaxisAPI)
    const id = typeof initialaxisAPI === 'number' ? initialaxisAPI : initialaxisAPI.ID
    const url = `${this.initialaxissUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<InitialAxisAPI>(url, initialaxisAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<InitialAxisAPI>('postInitialAxis'))
    );
  }
  
  /** POST: add a new initialaxis to the server */
  post(initialaxisdb: InitialAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI> {
    return this.postInitialAxis(initialaxisdb, GONG__StackPath, frontRepo)
  }
  postInitialAxis(initialaxisdb: InitialAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<InitialAxisAPI>(this.initialaxissUrl, initialaxisdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted initialaxisdb id=${initialaxisdb.ID}`)
      }),
      catchError(this.handleError<InitialAxisAPI>('postInitialAxis'))
    );
  }

  /** DELETE: delete the initialaxisdb from the server */
  delete(initialaxisdb: InitialAxisAPI | number, GONG__StackPath: string): Observable<InitialAxisAPI> {
    return this.deleteInitialAxis(initialaxisdb, GONG__StackPath)
  }
  deleteInitialAxis(initialaxisdb: InitialAxisAPI | number, GONG__StackPath: string): Observable<InitialAxisAPI> {
    const id = typeof initialaxisdb === 'number' ? initialaxisdb : initialaxisdb.ID;
    const url = `${this.initialaxissUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<InitialAxisAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted initialaxisdb id=${id}`)),
      catchError(this.handleError<InitialAxisAPI>('deleteInitialAxis'))
    );
  }

  // updateFront copy initialaxis to a version with encoded pointers and update to the back
  updateFront(initialaxis: InitialAxis, GONG__StackPath: string): Observable<InitialAxisAPI> {
    let initialaxisAPI = new InitialAxisAPI
    CopyInitialAxisToInitialAxisAPI(initialaxis, initialaxisAPI)
    const id = typeof initialaxisAPI === 'number' ? initialaxisAPI : initialaxisAPI.ID
    const url = `${this.initialaxissUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<InitialAxisAPI>(url, initialaxisAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<InitialAxisAPI>('updateInitialAxis'))
    );
  }

  /** PUT: update the initialaxisdb on the server */
  update(initialaxisdb: InitialAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI> {
    return this.updateInitialAxis(initialaxisdb, GONG__StackPath, frontRepo)
  }
  updateInitialAxis(initialaxisdb: InitialAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<InitialAxisAPI> {
    const id = typeof initialaxisdb === 'number' ? initialaxisdb : initialaxisdb.ID;
    const url = `${this.initialaxissUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<InitialAxisAPI>(url, initialaxisdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated initialaxisdb id=${initialaxisdb.ID}`)
      }),
      catchError(this.handleError<InitialAxisAPI>('updateInitialAxis'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in InitialAxisService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("InitialAxisService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
