// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { NoteInfoAPI } from './noteinfo-api'
import { NoteInfo, CopyNoteInfoToNoteInfoAPI } from './noteinfo'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class NoteInfoService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  NoteInfoServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private noteinfosUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.noteinfosUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/noteinfos';
  }

  /** GET noteinfos from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI[]> {
    return this.getNoteInfos(GONG__StackPath, frontRepo)
  }
  getNoteInfos(GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<NoteInfoAPI[]>(this.noteinfosUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<NoteInfoAPI[]>('getNoteInfos', []))
      );
  }

  /** GET noteinfo by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI> {
    return this.getNoteInfo(id, GONG__StackPath, frontRepo)
  }
  getNoteInfo(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.noteinfosUrl}/${id}`;
    return this.http.get<NoteInfoAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched noteinfo id=${id}`)),
      catchError(this.handleError<NoteInfoAPI>(`getNoteInfo id=${id}`))
    );
  }

  // postFront copy noteinfo to a version with encoded pointers and post to the back
  postFront(noteinfo: NoteInfo, GONG__StackPath: string): Observable<NoteInfoAPI> {
    let noteinfoAPI = new NoteInfoAPI
    CopyNoteInfoToNoteInfoAPI(noteinfo, noteinfoAPI)
    const id = typeof noteinfoAPI === 'number' ? noteinfoAPI : noteinfoAPI.ID
    const url = `${this.noteinfosUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<NoteInfoAPI>(url, noteinfoAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<NoteInfoAPI>('postNoteInfo'))
    );
  }
  
  /** POST: add a new noteinfo to the server */
  post(noteinfodb: NoteInfoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI> {
    return this.postNoteInfo(noteinfodb, GONG__StackPath, frontRepo)
  }
  postNoteInfo(noteinfodb: NoteInfoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<NoteInfoAPI>(this.noteinfosUrl, noteinfodb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted noteinfodb id=${noteinfodb.ID}`)
      }),
      catchError(this.handleError<NoteInfoAPI>('postNoteInfo'))
    );
  }

  /** DELETE: delete the noteinfodb from the server */
  delete(noteinfodb: NoteInfoAPI | number, GONG__StackPath: string): Observable<NoteInfoAPI> {
    return this.deleteNoteInfo(noteinfodb, GONG__StackPath)
  }
  deleteNoteInfo(noteinfodb: NoteInfoAPI | number, GONG__StackPath: string): Observable<NoteInfoAPI> {
    const id = typeof noteinfodb === 'number' ? noteinfodb : noteinfodb.ID;
    const url = `${this.noteinfosUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<NoteInfoAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted noteinfodb id=${id}`)),
      catchError(this.handleError<NoteInfoAPI>('deleteNoteInfo'))
    );
  }

  // updateFront copy noteinfo to a version with encoded pointers and update to the back
  updateFront(noteinfo: NoteInfo, GONG__StackPath: string): Observable<NoteInfoAPI> {
    let noteinfoAPI = new NoteInfoAPI
    CopyNoteInfoToNoteInfoAPI(noteinfo, noteinfoAPI)
    const id = typeof noteinfoAPI === 'number' ? noteinfoAPI : noteinfoAPI.ID
    const url = `${this.noteinfosUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<NoteInfoAPI>(url, noteinfoAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<NoteInfoAPI>('updateNoteInfo'))
    );
  }

  /** PUT: update the noteinfodb on the server */
  update(noteinfodb: NoteInfoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI> {
    return this.updateNoteInfo(noteinfodb, GONG__StackPath, frontRepo)
  }
  updateNoteInfo(noteinfodb: NoteInfoAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<NoteInfoAPI> {
    const id = typeof noteinfodb === 'number' ? noteinfodb : noteinfodb.ID;
    const url = `${this.noteinfosUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<NoteInfoAPI>(url, noteinfodb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated noteinfodb id=${noteinfodb.ID}`)
      }),
      catchError(this.handleError<NoteInfoAPI>('updateNoteInfo'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in NoteInfoService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("NoteInfoService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
