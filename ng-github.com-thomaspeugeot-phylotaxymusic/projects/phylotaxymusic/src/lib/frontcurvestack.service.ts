// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { FrontCurveStackAPI } from './frontcurvestack-api'
import { FrontCurveStack, CopyFrontCurveStackToFrontCurveStackAPI } from './frontcurvestack'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'
import { FrontCurveAPI } from './frontcurve-api'
import { SpiralCircleAPI } from './spiralcircle-api'

@Injectable({
  providedIn: 'root'
})
export class FrontCurveStackService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  FrontCurveStackServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private frontcurvestacksUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.frontcurvestacksUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/frontcurvestacks';
  }

  /** GET frontcurvestacks from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI[]> {
    return this.getFrontCurveStacks(GONG__StackPath, frontRepo)
  }
  getFrontCurveStacks(GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<FrontCurveStackAPI[]>(this.frontcurvestacksUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<FrontCurveStackAPI[]>('getFrontCurveStacks', []))
      );
  }

  /** GET frontcurvestack by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI> {
    return this.getFrontCurveStack(id, GONG__StackPath, frontRepo)
  }
  getFrontCurveStack(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.frontcurvestacksUrl}/${id}`;
    return this.http.get<FrontCurveStackAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched frontcurvestack id=${id}`)),
      catchError(this.handleError<FrontCurveStackAPI>(`getFrontCurveStack id=${id}`))
    );
  }

  // postFront copy frontcurvestack to a version with encoded pointers and post to the back
  postFront(frontcurvestack: FrontCurveStack, GONG__StackPath: string): Observable<FrontCurveStackAPI> {
    let frontcurvestackAPI = new FrontCurveStackAPI
    CopyFrontCurveStackToFrontCurveStackAPI(frontcurvestack, frontcurvestackAPI)
    const id = typeof frontcurvestackAPI === 'number' ? frontcurvestackAPI : frontcurvestackAPI.ID
    const url = `${this.frontcurvestacksUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FrontCurveStackAPI>(url, frontcurvestackAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FrontCurveStackAPI>('postFrontCurveStack'))
    );
  }
  
  /** POST: add a new frontcurvestack to the server */
  post(frontcurvestackdb: FrontCurveStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI> {
    return this.postFrontCurveStack(frontcurvestackdb, GONG__StackPath, frontRepo)
  }
  postFrontCurveStack(frontcurvestackdb: FrontCurveStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FrontCurveStackAPI>(this.frontcurvestacksUrl, frontcurvestackdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted frontcurvestackdb id=${frontcurvestackdb.ID}`)
      }),
      catchError(this.handleError<FrontCurveStackAPI>('postFrontCurveStack'))
    );
  }

  /** DELETE: delete the frontcurvestackdb from the server */
  delete(frontcurvestackdb: FrontCurveStackAPI | number, GONG__StackPath: string): Observable<FrontCurveStackAPI> {
    return this.deleteFrontCurveStack(frontcurvestackdb, GONG__StackPath)
  }
  deleteFrontCurveStack(frontcurvestackdb: FrontCurveStackAPI | number, GONG__StackPath: string): Observable<FrontCurveStackAPI> {
    const id = typeof frontcurvestackdb === 'number' ? frontcurvestackdb : frontcurvestackdb.ID;
    const url = `${this.frontcurvestacksUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<FrontCurveStackAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted frontcurvestackdb id=${id}`)),
      catchError(this.handleError<FrontCurveStackAPI>('deleteFrontCurveStack'))
    );
  }

  // updateFront copy frontcurvestack to a version with encoded pointers and update to the back
  updateFront(frontcurvestack: FrontCurveStack, GONG__StackPath: string): Observable<FrontCurveStackAPI> {
    let frontcurvestackAPI = new FrontCurveStackAPI
    CopyFrontCurveStackToFrontCurveStackAPI(frontcurvestack, frontcurvestackAPI)
    const id = typeof frontcurvestackAPI === 'number' ? frontcurvestackAPI : frontcurvestackAPI.ID
    const url = `${this.frontcurvestacksUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<FrontCurveStackAPI>(url, frontcurvestackAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FrontCurveStackAPI>('updateFrontCurveStack'))
    );
  }

  /** PUT: update the frontcurvestackdb on the server */
  update(frontcurvestackdb: FrontCurveStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI> {
    return this.updateFrontCurveStack(frontcurvestackdb, GONG__StackPath, frontRepo)
  }
  updateFrontCurveStack(frontcurvestackdb: FrontCurveStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveStackAPI> {
    const id = typeof frontcurvestackdb === 'number' ? frontcurvestackdb : frontcurvestackdb.ID;
    const url = `${this.frontcurvestacksUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<FrontCurveStackAPI>(url, frontcurvestackdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated frontcurvestackdb id=${frontcurvestackdb.ID}`)
      }),
      catchError(this.handleError<FrontCurveStackAPI>('updateFrontCurveStack'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in FrontCurveStackService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("FrontCurveStackService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
