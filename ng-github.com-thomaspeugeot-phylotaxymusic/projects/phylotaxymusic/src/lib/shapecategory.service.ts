// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { ShapeCategoryAPI } from './shapecategory-api'
import { ShapeCategory, CopyShapeCategoryToShapeCategoryAPI } from './shapecategory'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class ShapeCategoryService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  ShapeCategoryServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private shapecategorysUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.shapecategorysUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/shapecategorys';
  }

  /** GET shapecategorys from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI[]> {
    return this.getShapeCategorys(GONG__StackPath, frontRepo)
  }
  getShapeCategorys(GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<ShapeCategoryAPI[]>(this.shapecategorysUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<ShapeCategoryAPI[]>('getShapeCategorys', []))
      );
  }

  /** GET shapecategory by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI> {
    return this.getShapeCategory(id, GONG__StackPath, frontRepo)
  }
  getShapeCategory(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.shapecategorysUrl}/${id}`;
    return this.http.get<ShapeCategoryAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched shapecategory id=${id}`)),
      catchError(this.handleError<ShapeCategoryAPI>(`getShapeCategory id=${id}`))
    );
  }

  // postFront copy shapecategory to a version with encoded pointers and post to the back
  postFront(shapecategory: ShapeCategory, GONG__StackPath: string): Observable<ShapeCategoryAPI> {
    let shapecategoryAPI = new ShapeCategoryAPI
    CopyShapeCategoryToShapeCategoryAPI(shapecategory, shapecategoryAPI)
    const id = typeof shapecategoryAPI === 'number' ? shapecategoryAPI : shapecategoryAPI.ID
    const url = `${this.shapecategorysUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ShapeCategoryAPI>(url, shapecategoryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ShapeCategoryAPI>('postShapeCategory'))
    );
  }
  
  /** POST: add a new shapecategory to the server */
  post(shapecategorydb: ShapeCategoryAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI> {
    return this.postShapeCategory(shapecategorydb, GONG__StackPath, frontRepo)
  }
  postShapeCategory(shapecategorydb: ShapeCategoryAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<ShapeCategoryAPI>(this.shapecategorysUrl, shapecategorydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted shapecategorydb id=${shapecategorydb.ID}`)
      }),
      catchError(this.handleError<ShapeCategoryAPI>('postShapeCategory'))
    );
  }

  /** DELETE: delete the shapecategorydb from the server */
  delete(shapecategorydb: ShapeCategoryAPI | number, GONG__StackPath: string): Observable<ShapeCategoryAPI> {
    return this.deleteShapeCategory(shapecategorydb, GONG__StackPath)
  }
  deleteShapeCategory(shapecategorydb: ShapeCategoryAPI | number, GONG__StackPath: string): Observable<ShapeCategoryAPI> {
    const id = typeof shapecategorydb === 'number' ? shapecategorydb : shapecategorydb.ID;
    const url = `${this.shapecategorysUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<ShapeCategoryAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted shapecategorydb id=${id}`)),
      catchError(this.handleError<ShapeCategoryAPI>('deleteShapeCategory'))
    );
  }

  // updateFront copy shapecategory to a version with encoded pointers and update to the back
  updateFront(shapecategory: ShapeCategory, GONG__StackPath: string): Observable<ShapeCategoryAPI> {
    let shapecategoryAPI = new ShapeCategoryAPI
    CopyShapeCategoryToShapeCategoryAPI(shapecategory, shapecategoryAPI)
    const id = typeof shapecategoryAPI === 'number' ? shapecategoryAPI : shapecategoryAPI.ID
    const url = `${this.shapecategorysUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<ShapeCategoryAPI>(url, shapecategoryAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<ShapeCategoryAPI>('updateShapeCategory'))
    );
  }

  /** PUT: update the shapecategorydb on the server */
  update(shapecategorydb: ShapeCategoryAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI> {
    return this.updateShapeCategory(shapecategorydb, GONG__StackPath, frontRepo)
  }
  updateShapeCategory(shapecategorydb: ShapeCategoryAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<ShapeCategoryAPI> {
    const id = typeof shapecategorydb === 'number' ? shapecategorydb : shapecategorydb.ID;
    const url = `${this.shapecategorysUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<ShapeCategoryAPI>(url, shapecategorydb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated shapecategorydb id=${shapecategorydb.ID}`)
      }),
      catchError(this.handleError<ShapeCategoryAPI>('updateShapeCategory'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in ShapeCategoryService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("ShapeCategoryService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
