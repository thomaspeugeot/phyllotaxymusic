// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { VerticalAxisAPI } from './verticalaxis-api'
import { VerticalAxis, CopyVerticalAxisToVerticalAxisAPI } from './verticalaxis'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class VerticalAxisService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  VerticalAxisServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private verticalaxissUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.verticalaxissUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/verticalaxiss';
  }

  /** GET verticalaxiss from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI[]> {
    return this.getVerticalAxiss(GONG__StackPath, frontRepo)
  }
  getVerticalAxiss(GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<VerticalAxisAPI[]>(this.verticalaxissUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<VerticalAxisAPI[]>('getVerticalAxiss', []))
      );
  }

  /** GET verticalaxis by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI> {
    return this.getVerticalAxis(id, GONG__StackPath, frontRepo)
  }
  getVerticalAxis(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.verticalaxissUrl}/${id}`;
    return this.http.get<VerticalAxisAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched verticalaxis id=${id}`)),
      catchError(this.handleError<VerticalAxisAPI>(`getVerticalAxis id=${id}`))
    );
  }

  // postFront copy verticalaxis to a version with encoded pointers and post to the back
  postFront(verticalaxis: VerticalAxis, GONG__StackPath: string): Observable<VerticalAxisAPI> {
    let verticalaxisAPI = new VerticalAxisAPI
    CopyVerticalAxisToVerticalAxisAPI(verticalaxis, verticalaxisAPI)
    const id = typeof verticalaxisAPI === 'number' ? verticalaxisAPI : verticalaxisAPI.ID
    const url = `${this.verticalaxissUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<VerticalAxisAPI>(url, verticalaxisAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<VerticalAxisAPI>('postVerticalAxis'))
    );
  }
  
  /** POST: add a new verticalaxis to the server */
  post(verticalaxisdb: VerticalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI> {
    return this.postVerticalAxis(verticalaxisdb, GONG__StackPath, frontRepo)
  }
  postVerticalAxis(verticalaxisdb: VerticalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<VerticalAxisAPI>(this.verticalaxissUrl, verticalaxisdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted verticalaxisdb id=${verticalaxisdb.ID}`)
      }),
      catchError(this.handleError<VerticalAxisAPI>('postVerticalAxis'))
    );
  }

  /** DELETE: delete the verticalaxisdb from the server */
  delete(verticalaxisdb: VerticalAxisAPI | number, GONG__StackPath: string): Observable<VerticalAxisAPI> {
    return this.deleteVerticalAxis(verticalaxisdb, GONG__StackPath)
  }
  deleteVerticalAxis(verticalaxisdb: VerticalAxisAPI | number, GONG__StackPath: string): Observable<VerticalAxisAPI> {
    const id = typeof verticalaxisdb === 'number' ? verticalaxisdb : verticalaxisdb.ID;
    const url = `${this.verticalaxissUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<VerticalAxisAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted verticalaxisdb id=${id}`)),
      catchError(this.handleError<VerticalAxisAPI>('deleteVerticalAxis'))
    );
  }

  // updateFront copy verticalaxis to a version with encoded pointers and update to the back
  updateFront(verticalaxis: VerticalAxis, GONG__StackPath: string): Observable<VerticalAxisAPI> {
    let verticalaxisAPI = new VerticalAxisAPI
    CopyVerticalAxisToVerticalAxisAPI(verticalaxis, verticalaxisAPI)
    const id = typeof verticalaxisAPI === 'number' ? verticalaxisAPI : verticalaxisAPI.ID
    const url = `${this.verticalaxissUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<VerticalAxisAPI>(url, verticalaxisAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<VerticalAxisAPI>('updateVerticalAxis'))
    );
  }

  /** PUT: update the verticalaxisdb on the server */
  update(verticalaxisdb: VerticalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI> {
    return this.updateVerticalAxis(verticalaxisdb, GONG__StackPath, frontRepo)
  }
  updateVerticalAxis(verticalaxisdb: VerticalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<VerticalAxisAPI> {
    const id = typeof verticalaxisdb === 'number' ? verticalaxisdb : verticalaxisdb.ID;
    const url = `${this.verticalaxissUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<VerticalAxisAPI>(url, verticalaxisdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated verticalaxisdb id=${verticalaxisdb.ID}`)
      }),
      catchError(this.handleError<VerticalAxisAPI>('updateVerticalAxis'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in VerticalAxisService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("VerticalAxisService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
