// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SpiralCircleAPI } from './spiralcircle-api'
import { SpiralCircle, CopySpiralCircleToSpiralCircleAPI } from './spiralcircle'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'

@Injectable({
  providedIn: 'root'
})
export class SpiralCircleService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SpiralCircleServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private spiralcirclesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.spiralcirclesUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/spiralcircles';
  }

  /** GET spiralcircles from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI[]> {
    return this.getSpiralCircles(GONG__StackPath, frontRepo)
  }
  getSpiralCircles(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<SpiralCircleAPI[]>(this.spiralcirclesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SpiralCircleAPI[]>('getSpiralCircles', []))
      );
  }

  /** GET spiralcircle by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI> {
    return this.getSpiralCircle(id, GONG__StackPath, frontRepo)
  }
  getSpiralCircle(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.spiralcirclesUrl}/${id}`;
    return this.http.get<SpiralCircleAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched spiralcircle id=${id}`)),
      catchError(this.handleError<SpiralCircleAPI>(`getSpiralCircle id=${id}`))
    );
  }

  // postFront copy spiralcircle to a version with encoded pointers and post to the back
  postFront(spiralcircle: SpiralCircle, GONG__StackPath: string): Observable<SpiralCircleAPI> {
    let spiralcircleAPI = new SpiralCircleAPI
    CopySpiralCircleToSpiralCircleAPI(spiralcircle, spiralcircleAPI)
    const id = typeof spiralcircleAPI === 'number' ? spiralcircleAPI : spiralcircleAPI.ID
    const url = `${this.spiralcirclesUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralCircleAPI>(url, spiralcircleAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralCircleAPI>('postSpiralCircle'))
    );
  }
  
  /** POST: add a new spiralcircle to the server */
  post(spiralcircledb: SpiralCircleAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI> {
    return this.postSpiralCircle(spiralcircledb, GONG__StackPath, frontRepo)
  }
  postSpiralCircle(spiralcircledb: SpiralCircleAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralCircleAPI>(this.spiralcirclesUrl, spiralcircledb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted spiralcircledb id=${spiralcircledb.ID}`)
      }),
      catchError(this.handleError<SpiralCircleAPI>('postSpiralCircle'))
    );
  }

  /** DELETE: delete the spiralcircledb from the server */
  delete(spiralcircledb: SpiralCircleAPI | number, GONG__StackPath: string): Observable<SpiralCircleAPI> {
    return this.deleteSpiralCircle(spiralcircledb, GONG__StackPath)
  }
  deleteSpiralCircle(spiralcircledb: SpiralCircleAPI | number, GONG__StackPath: string): Observable<SpiralCircleAPI> {
    const id = typeof spiralcircledb === 'number' ? spiralcircledb : spiralcircledb.ID;
    const url = `${this.spiralcirclesUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SpiralCircleAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted spiralcircledb id=${id}`)),
      catchError(this.handleError<SpiralCircleAPI>('deleteSpiralCircle'))
    );
  }

  // updateFront copy spiralcircle to a version with encoded pointers and update to the back
  updateFront(spiralcircle: SpiralCircle, GONG__StackPath: string): Observable<SpiralCircleAPI> {
    let spiralcircleAPI = new SpiralCircleAPI
    CopySpiralCircleToSpiralCircleAPI(spiralcircle, spiralcircleAPI)
    const id = typeof spiralcircleAPI === 'number' ? spiralcircleAPI : spiralcircleAPI.ID
    const url = `${this.spiralcirclesUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SpiralCircleAPI>(url, spiralcircleAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralCircleAPI>('updateSpiralCircle'))
    );
  }

  /** PUT: update the spiralcircledb on the server */
  update(spiralcircledb: SpiralCircleAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI> {
    return this.updateSpiralCircle(spiralcircledb, GONG__StackPath, frontRepo)
  }
  updateSpiralCircle(spiralcircledb: SpiralCircleAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralCircleAPI> {
    const id = typeof spiralcircledb === 'number' ? spiralcircledb : spiralcircledb.ID;
    const url = `${this.spiralcirclesUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SpiralCircleAPI>(url, spiralcircledb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated spiralcircledb id=${spiralcircledb.ID}`)
      }),
      catchError(this.handleError<SpiralCircleAPI>('updateSpiralCircle'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SpiralCircleService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("SpiralCircleService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
