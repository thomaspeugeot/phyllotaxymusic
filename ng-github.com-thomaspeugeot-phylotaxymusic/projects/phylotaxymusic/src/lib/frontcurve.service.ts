// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { FrontCurveAPI } from './frontcurve-api'
import { FrontCurve, CopyFrontCurveToFrontCurveAPI } from './frontcurve'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports

@Injectable({
  providedIn: 'root'
})
export class FrontCurveService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  FrontCurveServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private frontcurvesUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.frontcurvesUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/frontcurves';
  }

  /** GET frontcurves from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI[]> {
    return this.getFrontCurves(GONG__StackPath, frontRepo)
  }
  getFrontCurves(GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<FrontCurveAPI[]>(this.frontcurvesUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<FrontCurveAPI[]>('getFrontCurves', []))
      );
  }

  /** GET frontcurve by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI> {
    return this.getFrontCurve(id, GONG__StackPath, frontRepo)
  }
  getFrontCurve(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.frontcurvesUrl}/${id}`;
    return this.http.get<FrontCurveAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched frontcurve id=${id}`)),
      catchError(this.handleError<FrontCurveAPI>(`getFrontCurve id=${id}`))
    );
  }

  // postFront copy frontcurve to a version with encoded pointers and post to the back
  postFront(frontcurve: FrontCurve, GONG__StackPath: string): Observable<FrontCurveAPI> {
    let frontcurveAPI = new FrontCurveAPI
    CopyFrontCurveToFrontCurveAPI(frontcurve, frontcurveAPI)
    const id = typeof frontcurveAPI === 'number' ? frontcurveAPI : frontcurveAPI.ID
    const url = `${this.frontcurvesUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FrontCurveAPI>(url, frontcurveAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FrontCurveAPI>('postFrontCurve'))
    );
  }
  
  /** POST: add a new frontcurve to the server */
  post(frontcurvedb: FrontCurveAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI> {
    return this.postFrontCurve(frontcurvedb, GONG__StackPath, frontRepo)
  }
  postFrontCurve(frontcurvedb: FrontCurveAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<FrontCurveAPI>(this.frontcurvesUrl, frontcurvedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted frontcurvedb id=${frontcurvedb.ID}`)
      }),
      catchError(this.handleError<FrontCurveAPI>('postFrontCurve'))
    );
  }

  /** DELETE: delete the frontcurvedb from the server */
  delete(frontcurvedb: FrontCurveAPI | number, GONG__StackPath: string): Observable<FrontCurveAPI> {
    return this.deleteFrontCurve(frontcurvedb, GONG__StackPath)
  }
  deleteFrontCurve(frontcurvedb: FrontCurveAPI | number, GONG__StackPath: string): Observable<FrontCurveAPI> {
    const id = typeof frontcurvedb === 'number' ? frontcurvedb : frontcurvedb.ID;
    const url = `${this.frontcurvesUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<FrontCurveAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted frontcurvedb id=${id}`)),
      catchError(this.handleError<FrontCurveAPI>('deleteFrontCurve'))
    );
  }

  // updateFront copy frontcurve to a version with encoded pointers and update to the back
  updateFront(frontcurve: FrontCurve, GONG__StackPath: string): Observable<FrontCurveAPI> {
    let frontcurveAPI = new FrontCurveAPI
    CopyFrontCurveToFrontCurveAPI(frontcurve, frontcurveAPI)
    const id = typeof frontcurveAPI === 'number' ? frontcurveAPI : frontcurveAPI.ID
    const url = `${this.frontcurvesUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<FrontCurveAPI>(url, frontcurveAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<FrontCurveAPI>('updateFrontCurve'))
    );
  }

  /** PUT: update the frontcurvedb on the server */
  update(frontcurvedb: FrontCurveAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI> {
    return this.updateFrontCurve(frontcurvedb, GONG__StackPath, frontRepo)
  }
  updateFrontCurve(frontcurvedb: FrontCurveAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<FrontCurveAPI> {
    const id = typeof frontcurvedb === 'number' ? frontcurvedb : frontcurvedb.ID;
    const url = `${this.frontcurvesUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<FrontCurveAPI>(url, frontcurvedb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated frontcurvedb id=${frontcurvedb.ID}`)
      }),
      catchError(this.handleError<FrontCurveAPI>('updateFrontCurve'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in FrontCurveService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("FrontCurveService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
