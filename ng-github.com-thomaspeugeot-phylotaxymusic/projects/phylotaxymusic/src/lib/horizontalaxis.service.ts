// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { HorizontalAxisAPI } from './horizontalaxis-api'
import { HorizontalAxis, CopyHorizontalAxisToHorizontalAxisAPI } from './horizontalaxis'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'

@Injectable({
  providedIn: 'root'
})
export class HorizontalAxisService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  HorizontalAxisServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private horizontalaxissUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.horizontalaxissUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/horizontalaxiss';
  }

  /** GET horizontalaxiss from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI[]> {
    return this.getHorizontalAxiss(GONG__StackPath, frontRepo)
  }
  getHorizontalAxiss(GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<HorizontalAxisAPI[]>(this.horizontalaxissUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<HorizontalAxisAPI[]>('getHorizontalAxiss', []))
      );
  }

  /** GET horizontalaxis by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI> {
    return this.getHorizontalAxis(id, GONG__StackPath, frontRepo)
  }
  getHorizontalAxis(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.horizontalaxissUrl}/${id}`;
    return this.http.get<HorizontalAxisAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched horizontalaxis id=${id}`)),
      catchError(this.handleError<HorizontalAxisAPI>(`getHorizontalAxis id=${id}`))
    );
  }

  // postFront copy horizontalaxis to a version with encoded pointers and post to the back
  postFront(horizontalaxis: HorizontalAxis, GONG__StackPath: string): Observable<HorizontalAxisAPI> {
    let horizontalaxisAPI = new HorizontalAxisAPI
    CopyHorizontalAxisToHorizontalAxisAPI(horizontalaxis, horizontalaxisAPI)
    const id = typeof horizontalaxisAPI === 'number' ? horizontalaxisAPI : horizontalaxisAPI.ID
    const url = `${this.horizontalaxissUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<HorizontalAxisAPI>(url, horizontalaxisAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<HorizontalAxisAPI>('postHorizontalAxis'))
    );
  }
  
  /** POST: add a new horizontalaxis to the server */
  post(horizontalaxisdb: HorizontalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI> {
    return this.postHorizontalAxis(horizontalaxisdb, GONG__StackPath, frontRepo)
  }
  postHorizontalAxis(horizontalaxisdb: HorizontalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<HorizontalAxisAPI>(this.horizontalaxissUrl, horizontalaxisdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted horizontalaxisdb id=${horizontalaxisdb.ID}`)
      }),
      catchError(this.handleError<HorizontalAxisAPI>('postHorizontalAxis'))
    );
  }

  /** DELETE: delete the horizontalaxisdb from the server */
  delete(horizontalaxisdb: HorizontalAxisAPI | number, GONG__StackPath: string): Observable<HorizontalAxisAPI> {
    return this.deleteHorizontalAxis(horizontalaxisdb, GONG__StackPath)
  }
  deleteHorizontalAxis(horizontalaxisdb: HorizontalAxisAPI | number, GONG__StackPath: string): Observable<HorizontalAxisAPI> {
    const id = typeof horizontalaxisdb === 'number' ? horizontalaxisdb : horizontalaxisdb.ID;
    const url = `${this.horizontalaxissUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<HorizontalAxisAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted horizontalaxisdb id=${id}`)),
      catchError(this.handleError<HorizontalAxisAPI>('deleteHorizontalAxis'))
    );
  }

  // updateFront copy horizontalaxis to a version with encoded pointers and update to the back
  updateFront(horizontalaxis: HorizontalAxis, GONG__StackPath: string): Observable<HorizontalAxisAPI> {
    let horizontalaxisAPI = new HorizontalAxisAPI
    CopyHorizontalAxisToHorizontalAxisAPI(horizontalaxis, horizontalaxisAPI)
    const id = typeof horizontalaxisAPI === 'number' ? horizontalaxisAPI : horizontalaxisAPI.ID
    const url = `${this.horizontalaxissUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<HorizontalAxisAPI>(url, horizontalaxisAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<HorizontalAxisAPI>('updateHorizontalAxis'))
    );
  }

  /** PUT: update the horizontalaxisdb on the server */
  update(horizontalaxisdb: HorizontalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI> {
    return this.updateHorizontalAxis(horizontalaxisdb, GONG__StackPath, frontRepo)
  }
  updateHorizontalAxis(horizontalaxisdb: HorizontalAxisAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<HorizontalAxisAPI> {
    const id = typeof horizontalaxisdb === 'number' ? horizontalaxisdb : horizontalaxisdb.ID;
    const url = `${this.horizontalaxissUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<HorizontalAxisAPI>(url, horizontalaxisdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated horizontalaxisdb id=${horizontalaxisdb.ID}`)
      }),
      catchError(this.handleError<HorizontalAxisAPI>('updateHorizontalAxis'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in HorizontalAxisService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("HorizontalAxisService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
