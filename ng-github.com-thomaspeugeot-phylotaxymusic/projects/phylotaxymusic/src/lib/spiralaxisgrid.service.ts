// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SpiralAxisGridAPI } from './spiralaxisgrid-api'
import { SpiralAxisGrid, CopySpiralAxisGridToSpiralAxisGridAPI } from './spiralaxisgrid'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'
import { SpiralAxisAPI } from './spiralaxis-api'

@Injectable({
  providedIn: 'root'
})
export class SpiralAxisGridService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SpiralAxisGridServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private spiralaxisgridsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.spiralaxisgridsUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/spiralaxisgrids';
  }

  /** GET spiralaxisgrids from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI[]> {
    return this.getSpiralAxisGrids(GONG__StackPath, frontRepo)
  }
  getSpiralAxisGrids(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<SpiralAxisGridAPI[]>(this.spiralaxisgridsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SpiralAxisGridAPI[]>('getSpiralAxisGrids', []))
      );
  }

  /** GET spiralaxisgrid by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI> {
    return this.getSpiralAxisGrid(id, GONG__StackPath, frontRepo)
  }
  getSpiralAxisGrid(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.spiralaxisgridsUrl}/${id}`;
    return this.http.get<SpiralAxisGridAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched spiralaxisgrid id=${id}`)),
      catchError(this.handleError<SpiralAxisGridAPI>(`getSpiralAxisGrid id=${id}`))
    );
  }

  // postFront copy spiralaxisgrid to a version with encoded pointers and post to the back
  postFront(spiralaxisgrid: SpiralAxisGrid, GONG__StackPath: string): Observable<SpiralAxisGridAPI> {
    let spiralaxisgridAPI = new SpiralAxisGridAPI
    CopySpiralAxisGridToSpiralAxisGridAPI(spiralaxisgrid, spiralaxisgridAPI)
    const id = typeof spiralaxisgridAPI === 'number' ? spiralaxisgridAPI : spiralaxisgridAPI.ID
    const url = `${this.spiralaxisgridsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralAxisGridAPI>(url, spiralaxisgridAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralAxisGridAPI>('postSpiralAxisGrid'))
    );
  }
  
  /** POST: add a new spiralaxisgrid to the server */
  post(spiralaxisgriddb: SpiralAxisGridAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI> {
    return this.postSpiralAxisGrid(spiralaxisgriddb, GONG__StackPath, frontRepo)
  }
  postSpiralAxisGrid(spiralaxisgriddb: SpiralAxisGridAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralAxisGridAPI>(this.spiralaxisgridsUrl, spiralaxisgriddb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted spiralaxisgriddb id=${spiralaxisgriddb.ID}`)
      }),
      catchError(this.handleError<SpiralAxisGridAPI>('postSpiralAxisGrid'))
    );
  }

  /** DELETE: delete the spiralaxisgriddb from the server */
  delete(spiralaxisgriddb: SpiralAxisGridAPI | number, GONG__StackPath: string): Observable<SpiralAxisGridAPI> {
    return this.deleteSpiralAxisGrid(spiralaxisgriddb, GONG__StackPath)
  }
  deleteSpiralAxisGrid(spiralaxisgriddb: SpiralAxisGridAPI | number, GONG__StackPath: string): Observable<SpiralAxisGridAPI> {
    const id = typeof spiralaxisgriddb === 'number' ? spiralaxisgriddb : spiralaxisgriddb.ID;
    const url = `${this.spiralaxisgridsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SpiralAxisGridAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted spiralaxisgriddb id=${id}`)),
      catchError(this.handleError<SpiralAxisGridAPI>('deleteSpiralAxisGrid'))
    );
  }

  // updateFront copy spiralaxisgrid to a version with encoded pointers and update to the back
  updateFront(spiralaxisgrid: SpiralAxisGrid, GONG__StackPath: string): Observable<SpiralAxisGridAPI> {
    let spiralaxisgridAPI = new SpiralAxisGridAPI
    CopySpiralAxisGridToSpiralAxisGridAPI(spiralaxisgrid, spiralaxisgridAPI)
    const id = typeof spiralaxisgridAPI === 'number' ? spiralaxisgridAPI : spiralaxisgridAPI.ID
    const url = `${this.spiralaxisgridsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SpiralAxisGridAPI>(url, spiralaxisgridAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralAxisGridAPI>('updateSpiralAxisGrid'))
    );
  }

  /** PUT: update the spiralaxisgriddb on the server */
  update(spiralaxisgriddb: SpiralAxisGridAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI> {
    return this.updateSpiralAxisGrid(spiralaxisgriddb, GONG__StackPath, frontRepo)
  }
  updateSpiralAxisGrid(spiralaxisgriddb: SpiralAxisGridAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralAxisGridAPI> {
    const id = typeof spiralaxisgriddb === 'number' ? spiralaxisgriddb : spiralaxisgriddb.ID;
    const url = `${this.spiralaxisgridsUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SpiralAxisGridAPI>(url, spiralaxisgriddb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated spiralaxisgriddb id=${spiralaxisgriddb.ID}`)
      }),
      catchError(this.handleError<SpiralAxisGridAPI>('updateSpiralAxisGrid'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SpiralAxisGridService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("SpiralAxisGridService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
