// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SpiralOriginAPI } from './spiralorigin-api'
import { SpiralOrigin, CopySpiralOriginToSpiralOriginAPI } from './spiralorigin'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'

@Injectable({
  providedIn: 'root'
})
export class SpiralOriginService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SpiralOriginServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private spiraloriginsUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.spiraloriginsUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/spiralorigins';
  }

  /** GET spiralorigins from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI[]> {
    return this.getSpiralOrigins(GONG__StackPath, frontRepo)
  }
  getSpiralOrigins(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<SpiralOriginAPI[]>(this.spiraloriginsUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SpiralOriginAPI[]>('getSpiralOrigins', []))
      );
  }

  /** GET spiralorigin by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI> {
    return this.getSpiralOrigin(id, GONG__StackPath, frontRepo)
  }
  getSpiralOrigin(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.spiraloriginsUrl}/${id}`;
    return this.http.get<SpiralOriginAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched spiralorigin id=${id}`)),
      catchError(this.handleError<SpiralOriginAPI>(`getSpiralOrigin id=${id}`))
    );
  }

  // postFront copy spiralorigin to a version with encoded pointers and post to the back
  postFront(spiralorigin: SpiralOrigin, GONG__StackPath: string): Observable<SpiralOriginAPI> {
    let spiraloriginAPI = new SpiralOriginAPI
    CopySpiralOriginToSpiralOriginAPI(spiralorigin, spiraloriginAPI)
    const id = typeof spiraloriginAPI === 'number' ? spiraloriginAPI : spiraloriginAPI.ID
    const url = `${this.spiraloriginsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralOriginAPI>(url, spiraloriginAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralOriginAPI>('postSpiralOrigin'))
    );
  }
  
  /** POST: add a new spiralorigin to the server */
  post(spiralorigindb: SpiralOriginAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI> {
    return this.postSpiralOrigin(spiralorigindb, GONG__StackPath, frontRepo)
  }
  postSpiralOrigin(spiralorigindb: SpiralOriginAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralOriginAPI>(this.spiraloriginsUrl, spiralorigindb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted spiralorigindb id=${spiralorigindb.ID}`)
      }),
      catchError(this.handleError<SpiralOriginAPI>('postSpiralOrigin'))
    );
  }

  /** DELETE: delete the spiralorigindb from the server */
  delete(spiralorigindb: SpiralOriginAPI | number, GONG__StackPath: string): Observable<SpiralOriginAPI> {
    return this.deleteSpiralOrigin(spiralorigindb, GONG__StackPath)
  }
  deleteSpiralOrigin(spiralorigindb: SpiralOriginAPI | number, GONG__StackPath: string): Observable<SpiralOriginAPI> {
    const id = typeof spiralorigindb === 'number' ? spiralorigindb : spiralorigindb.ID;
    const url = `${this.spiraloriginsUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SpiralOriginAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted spiralorigindb id=${id}`)),
      catchError(this.handleError<SpiralOriginAPI>('deleteSpiralOrigin'))
    );
  }

  // updateFront copy spiralorigin to a version with encoded pointers and update to the back
  updateFront(spiralorigin: SpiralOrigin, GONG__StackPath: string): Observable<SpiralOriginAPI> {
    let spiraloriginAPI = new SpiralOriginAPI
    CopySpiralOriginToSpiralOriginAPI(spiralorigin, spiraloriginAPI)
    const id = typeof spiraloriginAPI === 'number' ? spiraloriginAPI : spiraloriginAPI.ID
    const url = `${this.spiraloriginsUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SpiralOriginAPI>(url, spiraloriginAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralOriginAPI>('updateSpiralOrigin'))
    );
  }

  /** PUT: update the spiralorigindb on the server */
  update(spiralorigindb: SpiralOriginAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI> {
    return this.updateSpiralOrigin(spiralorigindb, GONG__StackPath, frontRepo)
  }
  updateSpiralOrigin(spiralorigindb: SpiralOriginAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralOriginAPI> {
    const id = typeof spiralorigindb === 'number' ? spiralorigindb : spiralorigindb.ID;
    const url = `${this.spiraloriginsUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SpiralOriginAPI>(url, spiralorigindb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated spiralorigindb id=${spiralorigindb.ID}`)
      }),
      catchError(this.handleError<SpiralOriginAPI>('updateSpiralOrigin'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SpiralOriginService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("SpiralOriginService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
