// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { BezierGridStackAPI } from './beziergridstack-api'
import { BezierGridStack, CopyBezierGridStackToBezierGridStackAPI } from './beziergridstack'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'
import { BezierGridAPI } from './beziergrid-api'

@Injectable({
  providedIn: 'root'
})
export class BezierGridStackService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  BezierGridStackServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private beziergridstacksUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.beziergridstacksUrl = origin + '/api/github.com/thomaspeugeot/phyllotaxymusic/go/v1/beziergridstacks';
  }

  /** GET beziergridstacks from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI[]> {
    return this.getBezierGridStacks(GONG__StackPath, frontRepo)
  }
  getBezierGridStacks(GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<BezierGridStackAPI[]>(this.beziergridstacksUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<BezierGridStackAPI[]>('getBezierGridStacks', []))
      );
  }

  /** GET beziergridstack by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI> {
    return this.getBezierGridStack(id, GONG__StackPath, frontRepo)
  }
  getBezierGridStack(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.beziergridstacksUrl}/${id}`;
    return this.http.get<BezierGridStackAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched beziergridstack id=${id}`)),
      catchError(this.handleError<BezierGridStackAPI>(`getBezierGridStack id=${id}`))
    );
  }

  // postFront copy beziergridstack to a version with encoded pointers and post to the back
  postFront(beziergridstack: BezierGridStack, GONG__StackPath: string): Observable<BezierGridStackAPI> {
    let beziergridstackAPI = new BezierGridStackAPI
    CopyBezierGridStackToBezierGridStackAPI(beziergridstack, beziergridstackAPI)
    const id = typeof beziergridstackAPI === 'number' ? beziergridstackAPI : beziergridstackAPI.ID
    const url = `${this.beziergridstacksUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BezierGridStackAPI>(url, beziergridstackAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BezierGridStackAPI>('postBezierGridStack'))
    );
  }

  /** POST: add a new beziergridstack to the server */
  post(beziergridstackdb: BezierGridStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI> {
    return this.postBezierGridStack(beziergridstackdb, GONG__StackPath, frontRepo)
  }
  postBezierGridStack(beziergridstackdb: BezierGridStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<BezierGridStackAPI>(this.beziergridstacksUrl, beziergridstackdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted beziergridstackdb id=${beziergridstackdb.ID}`)
      }),
      catchError(this.handleError<BezierGridStackAPI>('postBezierGridStack'))
    );
  }

  /** DELETE: delete the beziergridstackdb from the server */
  delete(beziergridstackdb: BezierGridStackAPI | number, GONG__StackPath: string): Observable<BezierGridStackAPI> {
    return this.deleteBezierGridStack(beziergridstackdb, GONG__StackPath)
  }
  deleteBezierGridStack(beziergridstackdb: BezierGridStackAPI | number, GONG__StackPath: string): Observable<BezierGridStackAPI> {
    const id = typeof beziergridstackdb === 'number' ? beziergridstackdb : beziergridstackdb.ID;
    const url = `${this.beziergridstacksUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<BezierGridStackAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted beziergridstackdb id=${id}`)),
      catchError(this.handleError<BezierGridStackAPI>('deleteBezierGridStack'))
    );
  }

  // updateFront copy beziergridstack to a version with encoded pointers and update to the back
  updateFront(beziergridstack: BezierGridStack, GONG__StackPath: string): Observable<BezierGridStackAPI> {
    let beziergridstackAPI = new BezierGridStackAPI
    CopyBezierGridStackToBezierGridStackAPI(beziergridstack, beziergridstackAPI)
    const id = typeof beziergridstackAPI === 'number' ? beziergridstackAPI : beziergridstackAPI.ID
    const url = `${this.beziergridstacksUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<BezierGridStackAPI>(url, beziergridstackAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<BezierGridStackAPI>('updateBezierGridStack'))
    );
  }

  /** PUT: update the beziergridstackdb on the server */
  update(beziergridstackdb: BezierGridStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI> {
    return this.updateBezierGridStack(beziergridstackdb, GONG__StackPath, frontRepo)
  }
  updateBezierGridStack(beziergridstackdb: BezierGridStackAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<BezierGridStackAPI> {
    const id = typeof beziergridstackdb === 'number' ? beziergridstackdb : beziergridstackdb.ID;
    const url = `${this.beziergridstacksUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<BezierGridStackAPI>(url, beziergridstackdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated beziergridstackdb id=${beziergridstackdb.ID}`)
      }),
      catchError(this.handleError<BezierGridStackAPI>('updateBezierGridStack'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in BezierGridStackService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("BezierGridStackService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
