// generated by ng_file_service_ts
import { Injectable, Component, Inject } from '@angular/core';
import { HttpClientModule, HttpParams } from '@angular/common/http';
import { HttpClient, HttpHeaders } from '@angular/common/http';
import { DOCUMENT, Location } from '@angular/common'

/*
 * Behavior subject
 */
import { BehaviorSubject } from 'rxjs'
import { Observable, of } from 'rxjs'
import { catchError, map, tap } from 'rxjs/operators'

import { SpiralBezierAPI } from './spiralbezier-api'
import { SpiralBezier, CopySpiralBezierToSpiralBezierAPI } from './spiralbezier'

import { FrontRepo, FrontRepoService } from './front-repo.service';

// insertion point for imports
import { ShapeCategoryAPI } from './shapecategory-api'

@Injectable({
  providedIn: 'root'
})
export class SpiralBezierService {

  // Kamar Raïmo: Adding a way to communicate between components that share information
  // so that they are notified of a change.
  SpiralBezierServiceChanged: BehaviorSubject<string> = new BehaviorSubject("");

  private spiralbeziersUrl: string

  constructor(
    private http: HttpClient,
    @Inject(DOCUMENT) private document: Document
  ) {
    // path to the service share the same origin with the path to the document
    // get the origin in the URL to the document
    let origin = this.document.location.origin

    // if debugging with ng, replace 4200 with 8080
    origin = origin.replace("4200", "8080")

    // compute path to the service
    this.spiralbeziersUrl = origin + '/api/github.com/thomaspeugeot/phylotaxymusic/go/v1/spiralbeziers';
  }

  /** GET spiralbeziers from the server */
  // gets is more robust to refactoring
  gets(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI[]> {
    return this.getSpiralBeziers(GONG__StackPath, frontRepo)
  }
  getSpiralBeziers(GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI[]> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    return this.http.get<SpiralBezierAPI[]>(this.spiralbeziersUrl, { params: params })
      .pipe(
        tap(),
        catchError(this.handleError<SpiralBezierAPI[]>('getSpiralBeziers', []))
      );
  }

  /** GET spiralbezier by id. Will 404 if id not found */
  // more robust API to refactoring
  get(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI> {
    return this.getSpiralBezier(id, GONG__StackPath, frontRepo)
  }
  getSpiralBezier(id: number, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)

    const url = `${this.spiralbeziersUrl}/${id}`;
    return this.http.get<SpiralBezierAPI>(url, { params: params }).pipe(
      // tap(_ => this.log(`fetched spiralbezier id=${id}`)),
      catchError(this.handleError<SpiralBezierAPI>(`getSpiralBezier id=${id}`))
    );
  }

  // postFront copy spiralbezier to a version with encoded pointers and post to the back
  postFront(spiralbezier: SpiralBezier, GONG__StackPath: string): Observable<SpiralBezierAPI> {
    let spiralbezierAPI = new SpiralBezierAPI
    CopySpiralBezierToSpiralBezierAPI(spiralbezier, spiralbezierAPI)
    const id = typeof spiralbezierAPI === 'number' ? spiralbezierAPI : spiralbezierAPI.ID
    const url = `${this.spiralbeziersUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralBezierAPI>(url, spiralbezierAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralBezierAPI>('postSpiralBezier'))
    );
  }
  
  /** POST: add a new spiralbezier to the server */
  post(spiralbezierdb: SpiralBezierAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI> {
    return this.postSpiralBezier(spiralbezierdb, GONG__StackPath, frontRepo)
  }
  postSpiralBezier(spiralbezierdb: SpiralBezierAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI> {

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.post<SpiralBezierAPI>(this.spiralbeziersUrl, spiralbezierdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`posted spiralbezierdb id=${spiralbezierdb.ID}`)
      }),
      catchError(this.handleError<SpiralBezierAPI>('postSpiralBezier'))
    );
  }

  /** DELETE: delete the spiralbezierdb from the server */
  delete(spiralbezierdb: SpiralBezierAPI | number, GONG__StackPath: string): Observable<SpiralBezierAPI> {
    return this.deleteSpiralBezier(spiralbezierdb, GONG__StackPath)
  }
  deleteSpiralBezier(spiralbezierdb: SpiralBezierAPI | number, GONG__StackPath: string): Observable<SpiralBezierAPI> {
    const id = typeof spiralbezierdb === 'number' ? spiralbezierdb : spiralbezierdb.ID;
    const url = `${this.spiralbeziersUrl}/${id}`;

    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.delete<SpiralBezierAPI>(url, httpOptions).pipe(
      tap(_ => this.log(`deleted spiralbezierdb id=${id}`)),
      catchError(this.handleError<SpiralBezierAPI>('deleteSpiralBezier'))
    );
  }

  // updateFront copy spiralbezier to a version with encoded pointers and update to the back
  updateFront(spiralbezier: SpiralBezier, GONG__StackPath: string): Observable<SpiralBezierAPI> {
    let spiralbezierAPI = new SpiralBezierAPI
    CopySpiralBezierToSpiralBezierAPI(spiralbezier, spiralbezierAPI)
    const id = typeof spiralbezierAPI === 'number' ? spiralbezierAPI : spiralbezierAPI.ID
    const url = `${this.spiralbeziersUrl}/${id}`;
    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    }

    return this.http.put<SpiralBezierAPI>(url, spiralbezierAPI, httpOptions).pipe(
      tap(_ => {
      }),
      catchError(this.handleError<SpiralBezierAPI>('updateSpiralBezier'))
    );
  }

  /** PUT: update the spiralbezierdb on the server */
  update(spiralbezierdb: SpiralBezierAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI> {
    return this.updateSpiralBezier(spiralbezierdb, GONG__StackPath, frontRepo)
  }
  updateSpiralBezier(spiralbezierdb: SpiralBezierAPI, GONG__StackPath: string, frontRepo: FrontRepo): Observable<SpiralBezierAPI> {
    const id = typeof spiralbezierdb === 'number' ? spiralbezierdb : spiralbezierdb.ID;
    const url = `${this.spiralbeziersUrl}/${id}`;


    let params = new HttpParams().set("GONG__StackPath", GONG__StackPath)
    let httpOptions = {
      headers: new HttpHeaders({ 'Content-Type': 'application/json' }),
      params: params
    };

    return this.http.put<SpiralBezierAPI>(url, spiralbezierdb, httpOptions).pipe(
      tap(_ => {
        // this.log(`updated spiralbezierdb id=${spiralbezierdb.ID}`)
      }),
      catchError(this.handleError<SpiralBezierAPI>('updateSpiralBezier'))
    );
  }

  /**
   * Handle Http operation that failed.
   * Let the app continue.
   * @param operation - name of the operation that failed
   * @param result - optional value to return as the observable result
   */
  private handleError<T>(operation = 'operation in SpiralBezierService', result?: T) {
    return (error: any): Observable<T> => {

      // TODO: send the error to remote logging infrastructure
      console.error("SpiralBezierService" + error); // log to console instead

      // TODO: better job of transforming error for user consumption
      this.log(`${operation} failed: ${error.message}`);

      // Let the app keep running by returning an empty result.
      return of(result as T);
    };
  }

  private log(message: string) {
    console.log(message)
  }
}
